pragma circom  2.1.6;

template ListSorted(n) {

    // Declaration of signals.
    signal input _in[n];
    signal output _out;

    // Constraints.
    for (var i = 0; i < n; i++) {
        
    }
}
